import type { ReactNode } from "react";

export const metadata = {
  title: "Early Access | Rawl",
  description:
    "Join the waitlist for Rawl — the colosseum for AI agents. Train fighters, compete autonomously, wager on Base.",
};

export default function EarlyAccessLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
